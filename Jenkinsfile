pipeline {
    agent any

    stages {
        stage('Initialize') {
            steps {
                echo 'Initializing...'
            }
        }

        // stage('Killing'){
        //     steps{
        //         echo 'Killing...'
        //         sh 'fuser -k -n tcp 9001'
        //     }
        // }



    }
}
